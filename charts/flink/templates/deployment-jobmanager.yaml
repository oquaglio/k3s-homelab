apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "flink.fullname" . }}-jobmanager
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "flink.labels" . | nindent 4 }}
    component: jobmanager
spec:
  replicas: {{ .Values.jobmanager.replicas }}
  selector:
    matchLabels:
      {{- include "flink.jobmanagerSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "flink.jobmanagerSelectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: jobmanager
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args: ["jobmanager"]
          ports:
            - containerPort: {{ .Values.jobmanager.rpcPort }}
              name: rpc
            - containerPort: {{ .Values.jobmanager.webUiPort }}
              name: web-ui
          volumeMounts:
            - name: flink-config
              mountPath: /opt/flink/conf/flink-conf.yaml
              subPath: flink-conf.yaml
          resources:
            requests:
              memory: {{ .Values.jobmanager.resources.requests.memory | quote }}
              cpu: {{ .Values.jobmanager.resources.requests.cpu | quote }}
            limits:
              memory: {{ .Values.jobmanager.resources.limits.memory | quote }}
              cpu: {{ .Values.jobmanager.resources.limits.cpu | quote }}
          {{- if .Values.healthcheck.enabled }}
          livenessProbe:
            httpGet:
              path: /
              port: web-ui
            initialDelaySeconds: {{ .Values.healthcheck.initialDelaySeconds }}
            periodSeconds: {{ .Values.healthcheck.periodSeconds }}
          readinessProbe:
            httpGet:
              path: /
              port: web-ui
            initialDelaySeconds: 15
            periodSeconds: 5
          {{- end }}
      volumes:
        - name: flink-config
          configMap:
            name: {{ include "flink.fullname" . }}-config
