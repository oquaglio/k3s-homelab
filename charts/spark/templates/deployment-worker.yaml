apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "spark.fullname" . }}-worker
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "spark.labels" . | nindent 4 }}
    component: worker
spec:
  replicas: {{ .Values.worker.replicas }}
  selector:
    matchLabels:
      {{- include "spark.workerSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "spark.workerSelectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.kafka.enabled }}
      initContainers:
        - name: download-kafka-jars
          image: busybox:latest
          command:
            - sh
            - -c
            - |
              MAVEN=https://repo1.maven.org/maven2
              SPARK_V={{ .Values.kafka.spark.version }}
              SCALA={{ .Values.kafka.spark.scala }}
              KAFKA_V={{ .Values.kafka.kafkaClientsVersion }}
              POOL_V={{ .Values.kafka.commonsPool2Version }}
              wget -q -P /jars "${MAVEN}/org/apache/spark/spark-sql-kafka-0-10_${SCALA}/${SPARK_V}/spark-sql-kafka-0-10_${SCALA}-${SPARK_V}.jar"
              wget -q -P /jars "${MAVEN}/org/apache/spark/spark-token-provider-kafka-assembly_${SCALA}/${SPARK_V}/spark-token-provider-kafka-assembly_${SCALA}-${SPARK_V}.jar"
              wget -q -P /jars "${MAVEN}/org/apache/kafka/kafka-clients/${KAFKA_V}/kafka-clients-${KAFKA_V}.jar"
              wget -q -P /jars "${MAVEN}/org/apache/commons/commons-pool2/${POOL_V}/commons-pool2-${POOL_V}.jar"
          volumeMounts:
            - name: kafka-jars
              mountPath: /jars
      {{- end }}
      containers:
        - name: spark-worker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.worker.webUiPort }}
              name: web-ui
          env:
            - name: SPARK_MODE
              value: "worker"
            - name: SPARK_MASTER_URL
              value: "spark://{{ include "spark.fullname" . }}-master:{{ .Values.master.sparkPort }}"
          {{- if .Values.kafka.enabled }}
          volumeMounts:
            - name: kafka-jars
              mountPath: /opt/bitnami/spark/custom-jars
          {{- end }}
          resources:
            requests:
              memory: {{ .Values.worker.resources.requests.memory | quote }}
              cpu: {{ .Values.worker.resources.requests.cpu | quote }}
            limits:
              memory: {{ .Values.worker.resources.limits.memory | quote }}
              cpu: {{ .Values.worker.resources.limits.cpu | quote }}
          {{- if .Values.healthcheck.enabled }}
          livenessProbe:
            tcpSocket:
              port: web-ui
            initialDelaySeconds: {{ .Values.healthcheck.initialDelaySeconds }}
            periodSeconds: {{ .Values.healthcheck.periodSeconds }}
          readinessProbe:
            tcpSocket:
              port: web-ui
            initialDelaySeconds: 15
            periodSeconds: 5
          {{- end }}
      {{- if .Values.kafka.enabled }}
      volumes:
        - name: kafka-jars
          emptyDir: {}
      {{- end }}
